export type Session = {
  id: string;
  email: string;
  name: string;
  photoURL: string;
};

export type UserObjectDB = {
  id: string;
  email: string;
  username: string;
  picture: string;
  name: string;
  verified_email: boolean;
};
